QueryPro实现文档1.0

运行在服务器1。
在QueryPro文件夹中。

通过配置文件Query.conf设置：监听端口号，最大询问字符串长度，返回结果标题最大长度。

业务流程：
main：监听服务器2，建立连接后，接收询问字符串，进行[处理]，通过Protocol_2返回结果，然后关闭连接。
处理：对字符串每个单词，在数据库中查找集合，求交集。排序。

询问字符串格式：
通过空格分割的只包含字母和数字的字符串。

返回结果格式：
URL,Title
